fastapi>=0.100.0
uvicorn[standard]>=0.22.0
libzim>=3.0.0
pydantic>=2.0
beautifulsoup4>=4.12.0
python-multipart>=0.0.6
sqlalchemy>=2.0.0
alembic>=1.13.0
# Offline AI chat uses the user's local Ollama daemon over HTTP (loopback),
# which ships its own llama.cpp binaries that work on older CPUs without
# AVX-512. No Python LLM dependencies needed — only stdlib urllib.
